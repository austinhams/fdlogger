# Create a PostgreSQL database named 'fdlogger' first:
#   createdb fdlogger
#
# Then copy this file to .env and adjust as needed:
#   cp .env.example .env

DATABASE_URL=postgresql://localhost:5432/fdlogger
PORT=3000
